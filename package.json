{
  "name": "hsk-study-app",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "HanziGo — lộ trình HSK 1–3 nghe, nói, đọc, viết bằng tiếng Việt.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check js/app.js && node --check js/data.js && node --check js/curriculum.js && npm test",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
