{
  "name": "hsk-study-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HanziGo — ứng dụng web học HSK 1 bằng tiếng Việt.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check js/app.js && node --check js/data.js && npm test",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
